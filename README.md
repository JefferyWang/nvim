# NVim 配置

## 使用

```bash
# 备份旧的 nvim 配置
mv ~/.config/nvim ~/.config/nvim_bak
# 清理缓存
rm -rf ~/.local/state/nvim
rm -rf ~/.local/share/nvim
# 克隆配置到本地
git clone https://github.com/JefferyWang/nvim.git ~/.config/nvim
```
